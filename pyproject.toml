[project]
name = "rest-api-drf"
version = "0.1.0"
description = "Build A Django Rest API with the DRF and deploy on algolia
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.7",
]
